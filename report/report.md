# 无人机 GPS 诱骗检测与安全分析实验报告

> 对应任务：2026 推免短期综合任务 ｜ 完成日期：2026-09-09 ｜ 复核更新：2026-09-10
> 所有数字来自本机运行输出（`outputs/metrics.csv`、`outputs/figs/`），可一键复现：`python scripts/run_all.py`

---

## 1. 问题定义

无人机导航依赖 GPS/IMU/气压计/磁罗盘等多源传感器融合。GPS 诱骗（spoofing）通过伪造
定位信号让飞控"相信"错误位置，攻击者可在 EKF 等融合滤波层面隐蔽地把无人机拉离航线。
本任务关注三个问题：

1. **检测**：给定一段飞行时间序列，判断其中是否发生 GPS 诱骗，以及发生的时区间；
2. **检测根据**：检测器输出置信度与报警区间，支持解释异常来源（哪个传感器一致性出现分歧）；
3. **在线能力**：检测必须以因果流式（causal streaming）方式运行——每个时刻只能利用当前与
   过去的数据，不能偷看未来；推理时延应远小于实时节奏。

**正式化**：滑窗二分类。日志以 20Hz 重采样，2s 窗 / 0.5s 步长；每窗标签 1 当且仅当该窗
与攻击区间（数据集 `attack_enabled` 字段）重叠 ≥50%。评价指标：AUROC、AUPRC、F1、
漏检率（未报警的攻击航班比例）、误报率（正常窗被判攻击的比例）、检测延迟
（攻击开始时到首次报警的秒数）。

## 2. 数据集介绍与预处理

**数据集**：UAV-GPS-Spoofing-Dataset（IEEE INFOCOM 2024 论文配套）：
PX4-SITL + Gazebo-Classic 仿真采集（2023），250Hz，四类传感器
（GPS/IMU/气压计/磁罗盘），航线为直线/曲线/随机三种，各含 1 正常 + 2 攻击航班
的多样本。攻击方式为"隐蔽式"：每次 GPS 更新对上报位置做极小增量偏移，使 EKF
难察觉而位置逐渐偏离真值（攻击在起飞后 60–120s 内随机启动）。

**规模**：`PX4_Simulation_Data/Merged/` 共 **360** 个时间同步日志
（`time_usec, pressure_altitude, latitude_deg, longitude_deg, altitude, velocity,
velocity_east/north/up, attack_enabled, orientation_x/y/z/w, angular_velocity_x/y/z,
linear_acceleration_x/y/z, magnetic_field_x/y/z`），其中 216 个航班含攻击区间。

**预处理**（`scripts/make_dataset.py`，加载器见 `src/data_loader.py`）：

1. **列名自动推断**：正则映射字段（支持 x/y/z 拆分列与 `a;b;c` 向量字符串两种形态）；
2. **四元数 → 姿态角**：`orientation_x/y/z/w` 转 roll/pitch/yaw（rad）；
3. **统一单位与时基**：`time_usec` → 秒，按时间排序；缺失列以 NaN 保留；
4. **重采样**：线性插值到 20Hz 均匀网格（削采样，兼容不同传感器频率场景）；
5. **窗口化**：2s（40 样本）/ 0.5s 步长 → **89,804 窗 × 71 特征**，窗口正例率 52.7%
   （攻击时段长、且覆盖稳定巡航阶段，与隐蔽攻击"持续拉偏"语义一致）；
6. **按航班划分**：train 62,554 / val 8,533 / test 18,717 窗，对应 252 / 36 / 72 个航班；
   test 含 45 个攻击航班、27 个正常航班。模型只在 train 拟合，val 选择阈值，test 仅作最终评测，
   相邻窗口不会跨划分（`configs/split.json` 固定）。

## 3. 方法介绍

### 3.1 特征设计（L1 原始统计 / L2 物理一致性 / L3 时序聚合）

- **L1 原始统计**：11 个物理量（z-高度差、水平速度、垂速、加速度模、角速度模、姿态、
  抖动率）的均值/标准差/最值/斜率/峰度 —— 共 66 维；
- **L2 物理一致性**（多传感器交叉验证，5 维）：
  - `c_gps_baro`：GPS 高度 − 气压计高度（两独立高度源）
  - `c_imu_gps_acc`：IMU 加速度模（去重力） − GPS 速度微分得到的加速度模
  - `c_heading_vel`：速度矢向与 yaw 航向角差
  - `c_jump`：相邻采样水平位移的 99 分位（诱骗步进/突变）
  - `c_smoothness`：加速度二阶差分（平滑度异常）
- **L3 时序聚合**：即窗内统计（上述 L1 已在窗内实现）。

**核心假设**：诱骗只污染 GPS 链路，其他传感器仍反映真实运动 → 一致性残差应暴露攻击。
（结果见 5.4：该假设**部分成立**，是本次实验最有价值的负面发现。）

### 3.2 检测方法（5 个主模型 + 1 个消融）

| 模型 | 特征 | 训练方式 | 定位 |
|---|---|---|---|
| **M0** 阈值规则 | L2 一致性 5 维 | 正常窗 mean±3σ，越限即警 | 规则 Baseline |
| **M1** 随机森林 | 仅 L1（66 维） | 监督 | 学习 Baseline |
| **M1b** 随机森林 | L1+L2+L3（71 维） | 监督 | M1 消融（检验 L2 增益） |
| **M2** HistGradientBoosting | 全特征 | 监督 | 改进方案（GBDT） |
| **M3** BiLSTM | 原始传感器序列（40×9） | 监督，GPU | 表征学习对比 |
| **M4** 孤立森林 | 全特征 | **无监督**（仅正常窗拟合） | 现实标签稀缺场景 |

所有监督模型训练/评测共用同一特征管线、同一划分；随机种子固定 42。

## 4. 实验设置、指标和运行环境

- **环境**：Windows 11 / Python 3.12.1；GPU NVIDIA GeForce RTX 4050 Laptop（6GB, CUDA 12.7,
  驱动 566.24）；pytorch 2.9.1+cu126；scikit-learn 1.7+；pandas 2.x（`docs/environment.md`）。
- **指标**：AUROC/AUPRC（测试窗级）、F1/精确率/召回率（阈值在验证集滑阈 0.05–0.95 取最优）、
  漏检率（攻击航班未被检测比例）、误报率（正常窗报警比例）、检测延迟（航班级：攻击段
  首个 1 标签窗 → 首次报警窗的时间差，均值/中位数）。
- **M3**：每特征标准化；LSTM(hidden=64, BiLSTM) + 早停（patience=3）；正常/攻击各 ≤1 万窗
  分层采样控制显存；15–20 epochs，Adam 1e-3。
- **训练耗时**：M0 9.5s｜M1 65.1s｜M1b 62.0s｜M2 13.1s｜M3 75.4s（GPU）｜M4 3.8s。

## 5. 实验结果

### 5.1 总指标表（test 集，取自 `outputs/metrics.csv`）

| 模型 | AUROC | AUPRC | F1 | P | R | 漏检率 | 误报率 | 延迟均值(s) |
|---|---|---|---|---|---|---|---|---|
| M0 阈值(3σ) | 0.406 | 0.531 | 0.562 | 0.535 | 0.593 | 0.0 | 0.716 | 0.667 |
| M1 RF(@L1) | 0.777 | 0.803 | 0.790 | 0.668 | 0.966 | 0.0 | 0.668 | 0.233 |
| M1b RF(@全特征) | 0.776 | 0.802 | 0.783 | 0.649 | **0.987** | 0.0 | 0.744 | **0.089** |
| M2 HistGB(全特征) | **0.777** | **0.807** | **0.797** | **0.696** | 0.933 | 0.0 | **0.568** | 0.211 |
| M3 BiLSTM | 0.724 | 0.749 | 0.763 | 0.651 | 0.921 | 0.0 | 0.687 | 0.722 |
| M4 孤立森林(无监督) | 0.337 | 0.476 | 0.688 | 0.568 | 0.871 | 0.0 | 0.922 | 0.056 |

阈值全部由 val 选择。漏检率均为 0，但这是“一个航班只要有一个攻击窗报警即检出”的宽松定义；
窗口级误报率仍高达 56.8%–92.2%，因此这些模型只能视为研究原型，不能宣称已经可部署。

### 5.2 主要结论

1. **监督方法的排序能力优于规则法**：M1/M2 AUROC 约 0.777，M0 为 0.406；但监督方法
   的测试误报率仍有 56.8%–74.4%，说明模型同时学到了航线或飞行阶段差异。
2. **M2 是当前表现最好的研究基线，不是可部署方案**：AUPRC 0.807、F1 0.797、
   误报率 0.568。实际部署必须先控制误报并做跨域验证。
3. **特征工程暂时优于序列学习**：严格隔离航班后，M3 AUROC 0.724、F1 0.763，
   低于 M1/M2；在本数据与 2 秒上下文下，BiLSTM 没有体现优势。
4. **L2 一致性特征几乎无独立增益**：M1b 与 M1 AUROC 分别为 0.776/0.777，说明当前
   一致性残差尚未抓住隐蔽水平位置偏移的核心信号。
5. **无监督方法当前不可用**：M4 AUROC 0.337、误报率 0.922，详见 5.4。
6. **在线机制成立，检测效果仍不合格**：逐窗推理只使用历史数据，单航段约 23–25 秒处理完，
   但正常航班也可能报警，因此在线接口只能作为工程演示，不能作为安全部署结论。

### 5.3 在线检测演示路径

```bash
python -m src.cli predict --input <飞行CSV>            # 默认模型 outputs/models/m1.joblib
uvicorn src.server:app --port 8000                     # FastAPI: /healthz /predict /predict_file
```

输出示例如下（攻击航班抽样；正常航班误报案例见 5.4）：

```json
{"flight": "log_21_04_2023_07_21_03.csv", "spoofing": true, "threshold": 0.25, "confidence": 0.965,
 "alarm_intervals": [[2938.6, 3163.1]], "n_windows": 456, "runtime_s": 22.7}
```

### 5.4 失败与局限（如实记录，含分析）

1. **旧版评测泄漏已纠正**：旧版 `predict_proba_all` 没有保留 `split`，评测函数因此把全部
   窗口当作评测集，产生了 M1 AUROC 0.988 的虚高结果。本版补回 split 元数据，并固定
   train/val/test 航班互斥；纠正后的 M1 测试 AUROC 为 0.777。旧数字不再使用。
2. **M0 阈值 AUROC<0.5（0.406）**：初版在**全训练窗**（含攻击）上拟合 mean±3σ，
   统计被攻击样本污染；修正为**仅正常段拟合**后仍 <0.5。进一步诊断（单特征 AUC）：
   `c_gps_baro=0.500`（攻击只偏移水平位置，GPS 与气压计高度始终一致——一致性特征失效）、
   `c_jump=0.371`（**反向**：诱骗的缓步偏移反而使位置更平滑；正常巡航段有微扰）
   → 阈值法在一个"特征反向"的维度上取绝对值越限必然误判。
3. **M4 假阳性爆炸（误报率 92.2%）**：孤立森林把**正常航班起飞/降落/转弯等强动态段**
   视为异常，攻击巡航段反而"平静"→ 分数与标签反相关（AUROC 0.337）。
   启示：无监督异常检测必须显式建模**飞行阶段**（起飞/巡航/降落），仅靠分布异常
   无法区分"攻击"与"正常机动"。
4. **数据本身是仿真**：无真实射频信道噪声、无真实 GPS 信号欺骗延迟/多径，
   且 `attack_enabled` 是攻击脚本开关而非"GPS 观测开始偏差"的精确时刻，
   延迟指标以此为基准存在上界估计。真实射频环境结论需外推验证。
5. **类分布**：窗级正例率 52.7%，攻击覆盖大部分巡航期；因此 F1 和航班漏检率容易显得
   乐观，窗口级误报率与跨航线结果更能暴露实际泛化问题。
6. **在线正常样例会误报**：`Curved/Normal/log_21_04_2023_07_12_27.csv` 输出
   `spoofing=true, confidence=0.825`，与测试误报率一致。这一案例保留在报告中，防止把接口
   可运行误写成检测效果已可用。

### 5.5 跨航线泛化实验

固定 M1，依次将某一种航线全部作为 test，其余航线再按航班分 train/val：

| 留出航线 | AUROC | F1 | 误报率 | 召回率 |
|---|---:|---:|---:|---:|
| Straight | 0.627 | 0.122 | 0.021 | 0.066 |
| Curved | 0.535 | 0.787 | 0.887 | 0.935 |
| Random | 0.533 | 0.436 | 0.970 | 0.999 |

结果表明模型存在明显航线依赖：留出 Curved/Random 时 AUROC 接近随机且误报极高；
留出 Straight 时误报较低，但大部分攻击未检出。这比随机航班划分更严格，也更接近未知场景。

### 5.6 下一步（若继续研究）

- 对 GPS 高度欺骗/组合攻击扩展 L2 特征（真实假说里高度一致性会在"全维欺骗"下失效）；
- 无监督路线：先做飞行阶段分割（真值/降落地标），再在巡航段内做异常检测；
- 按飞行阶段建模，并设计对航线变化不敏感的相对残差；
- M3 尝试更长时间上下文（8–10s）与 attention；在线服务增加批次流式与窗口重叠模糊处理；
- 引入真实 GPS 原始信号级数据（RTKLIB/软件接收机输出）做跨域验证。

## 6. AI 工具、开源代码和外部资源使用声明

- **AI 辅助**：本项目使用 Claude Code（Claude Agent SDK）与 OpenAI Codex 辅助完成——包括
  方案与实验设计、代码编写与调试、性能分析（如 RF 单行预测线程池开销、浮点键对齐）、
  报告草拟。所有实验数字取自本机实际运行输出（`outputs/`），AI 未生成或美化的任何数字；
  每步代码由作业者复核后运行验证（`pytest 28/28` 通过，含真实数据的 CLI/API 冒烟）。
- **开源库**：pandas、numpy、scikit-learn、matplotlib、seaborn、fastapi、uvicorn、
  pyarrow、pytest、torch（`requirements.txt`）。Google Drive 下载使用 gdown。
- **数据集/参考资料**：
  - UAV-GPS-Spoofing-Dataset（Anthony Finn 等，本文档 2 节）；
  - 论文：Finn, Jia, Li, Yuan, *Detecting Stealthy GPS Spoofing Attack Against UAVs Using
    Onboard Sensors*, IEEE INFOCOM WKSHPS 2024（仅参考实验设计与特征表，检测代码为自主实现）；
  - PX4 SITL / Gazebo 官方文档（理解日志字段语义）。
- **未使用**：本任务数据集中原作者论文实现或任何第三方检测器代码；结果对比表仅含自主实现。

## 附录：复现命令

```bash
pip install -r requirements.txt
python scripts/run_all.py          # 下载(2.6GB)→特征→训练→评测→图表 ≈ 20–30 分钟
python scripts/run_route_holdout.py # 跨航线泛化实验
python -m pytest tests/            # 28 个自检用例
python -m src.cli predict --input data/raw/PX4_Simulation_Data/Merged/Curved/Attacked/log_21_04_2023_07_21_03.csv
```
